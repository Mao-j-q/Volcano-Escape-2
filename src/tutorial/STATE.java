package tutorial;

//Enum called STATE that represents the current program state that the game is currently in. 

public enum STATE {
	Menu(), Game(), Settings(), End(), Help(), Boss(), Shop(), Pause;
}
